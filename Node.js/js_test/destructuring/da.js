const object = {a: 1, b: 2};

// 객체에서 사용
// const {a, b} = object;

// console.log(a);
// console.log(b);

const array = [1, 2];

const [one, two] = array;

console.log(one, two)

// 객체안의 key값으로 불러올수 있음